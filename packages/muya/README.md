# muya —— 块级编辑内核

墨记的块级 Markdown 编辑内核（TypeScript，构建发布为 `@muyajs/core`）：

- 基于块（block-based）的文档模型与渲染管线
- CommonMark / GFM 语法支持，增量块更新
- 表格、HTML 内嵌渲染（iframe 沙箱预览）等扩展块
- 面向中文输入优化的 IME 组合处理

> 内核仍在活跃开发中，小版本间 API 可能调整。
